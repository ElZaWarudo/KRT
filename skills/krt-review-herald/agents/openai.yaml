interface:
  display_name: "krt-review-herald"
  short_description: "Resolve PR feedback with fixes, replies, and thread closeout"
  default_prompt: "Use krt-review-herald to triage PR review feedback, apply safe fixes, verify the result, draft replies, and post approved thread replies or resolutions when appropriate."
