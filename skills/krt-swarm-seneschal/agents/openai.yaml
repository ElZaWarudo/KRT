interface:
  display_name: "krt-swarm-seneschal"
  short_description: "Plan and reconcile bounded worker waves"
  default_prompt: "Use krt-swarm-seneschal ($krt-swarm-seneschal) to plan a bounded worker wave, dispatch approved units, reconcile evidence, or report status."

policy:
  allow_implicit_invocation: false
